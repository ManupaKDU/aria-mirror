declare module './.constants.js' {
  const TOKEN: string;
  const ARIA_SECRET: string;
  const ARIA_DOWNLOAD_LOCATION: string;
  const ARIA_DOWNLOAD_LOCATION_ROOT: string;
  const ARIA_FILTERED_DOMAINS: string[];
  const ARIA_FILTERED_FILENAMES: string[];
  const ARIA_PORT: number;
  const GDRIVE_PARENT_DIR_ID: string;
  const SUDO_USERS: number[];
  const AUTHORIZED_CHATS: number[];
  const STATUS_UPDATE_INTERVAL_MS: number;
  const DRIVE_FILE_PRIVATE: {
    ENABLED: boolean;
    EMAILS: string[];
  };
  const DOWNLOAD_NOTIFY_TARGET: {
    enabled: boolean;
    host: string;
    port: number;
    path: string;
  };
  const COMMANDS_USE_BOT_NAME: {
    ENABLED: boolean;
    NAME: string;
  };
  const IS_TEAM_DRIVE: boolean;
  export = constants;
  const constants: {
    TOKEN: string;
    ARIA_SECRET: string;
    ARIA_DOWNLOAD_LOCATION: string;
    ARIA_DOWNLOAD_LOCATION_ROOT: string;
    ARIA_FILTERED_DOMAINS: string[];
    ARIA_FILTERED_FILENAMES: string[];
    ARIA_PORT: number;
    GDRIVE_PARENT_DIR_ID: string;
    SUDO_USERS: number[];
    AUTHORIZED_CHATS: number[];
    STATUS_UPDATE_INTERVAL_MS: number;
    DRIVE_FILE_PRIVATE: {
      ENABLED: boolean;
      EMAILS: string[];
    };
    DOWNLOAD_NOTIFY_TARGET: {
      enabled: boolean;
      host: string;
      port: number;
      path: string;
    };
    COMMANDS_USE_BOT_NAME: {
      ENABLED: boolean;
      NAME: string;
    };
    IS_TEAM_DRIVE: boolean;
  };
}

declare module './.constants' {
  const TOKEN: string;
  const ARIA_SECRET: string;
  const ARIA_DOWNLOAD_LOCATION: string;
  const ARIA_DOWNLOAD_LOCATION_ROOT: string;
  const ARIA_FILTERED_DOMAINS: string[];
  const ARIA_FILTERED_FILENAMES: string[];
  const ARIA_PORT: number;
  const GDRIVE_PARENT_DIR_ID: string;
  const SUDO_USERS: number[];
  const AUTHORIZED_CHATS: number[];
  const STATUS_UPDATE_INTERVAL_MS: number;
  const DRIVE_FILE_PRIVATE: {
    ENABLED: boolean;
    EMAILS: string[];
  };
  const DOWNLOAD_NOTIFY_TARGET: {
    enabled: boolean;
    host: string;
    port: number;
    path: string;
  };
  const COMMANDS_USE_BOT_NAME: {
    ENABLED: boolean;
    NAME: string;
  };
  const IS_TEAM_DRIVE: boolean;
  export = constants;
  const constants: {
    TOKEN: string;
    ARIA_SECRET: string;
    ARIA_DOWNLOAD_LOCATION: string;
    ARIA_DOWNLOAD_LOCATION_ROOT: string;
    ARIA_FILTERED_DOMAINS: string[];
    ARIA_FILTERED_FILENAMES: string[];
    ARIA_PORT: number;
    GDRIVE_PARENT_DIR_ID: string;
    SUDO_USERS: number[];
    AUTHORIZED_CHATS: number[];
    STATUS_UPDATE_INTERVAL_MS: number;
    DRIVE_FILE_PRIVATE: {
      ENABLED: boolean;
      EMAILS: string[];
    };
    DOWNLOAD_NOTIFY_TARGET: {
      enabled: boolean;
      host: string;
      port: number;
      path: string;
    };
    COMMANDS_USE_BOT_NAME: {
      ENABLED: boolean;
      NAME: string;
    };
    IS_TEAM_DRIVE: boolean;
  };
}
